services:
  cardano-uptime:
    build: .
    container_name: cardano-uptime-tracker
    ports:
      - "8080:3000"
    environment:
      - CEXPLORER_API_KEY=${CEXPLORER_API_KEY}
    restart: unless-stopped